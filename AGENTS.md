# AGENTS.md

## Repository expectations

- This project is being transformed into a mobile-first avatar social dating app.
- Do not port browser-specific UI directly.
- Use React Native + TypeScript compatible architecture.
- Keep domain logic separate from rendering.
- Prefer reusable shared types and event models.
- Use alias imports where possible.
- Propose a plan before large refactors.
